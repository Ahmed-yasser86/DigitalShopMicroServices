services:
  catalogdb:
    image: postgres:16-alpine
    container_name: catalogdb
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=CatalogDb
    ports:
      - "5433:5432"
    volumes:
      - postgres_catalog:/var/lib/postgresql/data

  Basketgdb:
    image: postgres:16-alpine
    container_name: Basketgdb
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=BasketDb
    ports:
      - "5443:5432"
    volumes:
      - postgres_Basket:/var/lib/postgresql/data

  distributechache:
    image: redis
    container_name: distributedcache
    restart: always
    ports:
      - "6379:6379"

  catalog.api:
    image: catalog.api:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__Database=Server=catalogdb;Port=5432;Database=CatalogDb;User Id=postgres;Password=postgres;Include Error Detail=true
    depends_on:
      - catalogdb
    ports:
      - "6000:8080"

  discount.grpc:
    image: discountgrpc:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__Database=Data Source=discountdb
    ports:
      - "6002:8080"

  basket.api:
    image: basketapi:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__Database=Server=Basketgdb;Port=5432;Database=BasketDb;User Id=postgres;Password=postgres;Include Error Detail=true
      - ConnectionStrings__Redis=distributechache:6379
      - GrpcSettings__DisscountUrl=http://discount.grpc:8080
    depends_on:
      - Basketgdb
      - distributechache
      - discount.grpc
    ports:
      - "6010:8080"

volumes:
  postgres_catalog:
  postgres_Basket: